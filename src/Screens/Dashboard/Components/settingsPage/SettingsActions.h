#pragma once
#include <QWidget>
#include <QPushButton>
#include <QHBoxLayout>

class SettingsActions : public QWidget {
    Q_OBJECT
public:
    explicit SettingsActions(QWidget* parent = nullptr);
};
